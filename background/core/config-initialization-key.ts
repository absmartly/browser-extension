// Worker-global name of the settled startup config-initialization promise.
// Shared with the e2e fixture, which waits on it before seeding storage.
export const CONFIG_INITIALIZATION_KEY = "__absmartlyConfigInitialization"
